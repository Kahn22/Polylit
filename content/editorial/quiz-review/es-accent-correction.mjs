import{readFileSync}from'node:fs';const x=JSON.parse(readFileSync(new URL('es-accent-input.json',import.meta.url))).entries[0];const lemma={id:'lem_es_aun_temporal_adverb',headword:'aún',partOfSpeech:'adverb'};
export default{version:1,id:'es-2026-09-19-37',language:'es',snapshot:'es-accent-input.json',entries:[{from:x.identity,metadataCorrection:true,target:{lemma,surface:{...x.surface,lemmaId:lemma.id},sense:{...x.sense,lemmaId:lemma.id}},occurrenceIds:'all',questions:[null,null,null],reason:'Reviewed source occurrence and all three existing questions: aún expresses temporal continuation (still); aun expresses even. Restore the accented temporal lemma under its own ID after the accent-stripping helper collided. Preserve the unchanged surface+sense mastery identity and all three correct quiz wordings.'}]};
